streamlit==1.22.0
pickle==<version_number>   # Pickle is part of the standard library and does not have a separate version.
time==<version_number>     # Time is part of the standard library and does not have a separate version.
requests==2.28.2
beautifulsoup4==4.12.3
langchain==0.3.13
python-dotenv==1.0.0
base64==<version_number>   # Base64 is part of the standard library and does not have a separate version.
faiss-cpu==1.9.0.post1
openai==1.58.1
unstructured==0.9.2
tiktoken==0.8.0
libmagic==1.0
python-magic==0.4.27
python-magic-bin==0.4.14
